import assetConfig from "../assets/asset.config.ts";

const LoginIcon: React.FC  = () => {
    return (
        <img src = {assetConfig.loginIcon} alt = "login-icon" />
    );


};

export default LoginIcon;
